// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // 你的 GitHub Pages 網址。
  // username.github.io 是「使用者站」，走根目錄，所以不需要設 base。
  site: "https://kidhh123.github.io",
});
